# Core Telegram bot
python-telegram-bot[job-queue]==21.6

# HTTP clients
httpx==0.27.2
aiohttp==3.10.10

# Data / numeric / ML
numpy==2.1.2
pandas==2.2.3
scikit-learn==1.5.2

# Sentiment analysis (offline, no API key required)
vaderSentiment==3.3.2

# Config / env
python-dotenv==1.0.1

# Feed parsing for news (RSS fallback, no API key required)
feedparser==6.0.11
